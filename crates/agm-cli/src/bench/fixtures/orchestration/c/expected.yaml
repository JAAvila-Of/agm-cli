# Adversarial case — vague multi-agent description. Only require orchestration with any summary.
expected_type: orchestration
must_validate: false
must_match_schema: false
required_fields:
  - field: summary
